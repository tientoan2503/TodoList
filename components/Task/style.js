import {StyleSheet} from 'react-native';

const styles = {
  todoItem: {
    flexDirection: 'row',
    alignItems: 'center',
    paddingVertical: 8,
    paddingHorizontal: 16,
  },
  textContent: {
    flex: 1,
  },
  iconRemove: {
    opacity: 0.5,
    padding: 6,
    alignItems: 'center',
    justifyContents: 'center' 
  }
};

export default styles;
